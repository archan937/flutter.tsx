name: sweep_probes
description: >-
  Generated probes for the 543-widget analyze sweep: one minimal usage per
  widget, transpiled from TSX and held to the same flutter_lints analyze gate
  as the golden fixtures.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.0
  flutter: '>=3.47.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
